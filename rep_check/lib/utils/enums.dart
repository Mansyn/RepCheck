enum Query { full, state, district }
enum Body { upper, lower }
enum Lookup { auto, manual }
enum ButtonType {
  facebook,
  github,
  apple,
  twitter,
  google,
  youtube,
  microsoft,
  mail,
  yahoo,
  amazon,
  web,
}
enum ImagePosition {
  left,
  right,
}
